# Resume-and-JD-Matcher
Resume Matcher AI is a FastAPI-based GenAI backend that analyzes PDF resumes against job descriptions. It extracts structured data using an LLM, applies Python-based skill matching, and returns match percentage, strengths, gaps, and improvement suggestions via validated REST APIs.
